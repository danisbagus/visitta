require("babel-register");

const path = require("path");

module.exports = {
  config: path.resolve("config", "sequelize.js"),
  "migrations-path": path.resolve("databases", "migrations"),
  "seeders-path": path.resolve("databases", "migrations"),
};
